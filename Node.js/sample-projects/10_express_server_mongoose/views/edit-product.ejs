<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('includes/navbar.ejs') %>
    <main>
        <form class="edit-product-form" action="/admin/<%= action %>-product" method="POST">
            <div class="form-control">
                <label>Title</label>
                <input type="text" name="title" id="title" value="<%= product.title %>"/>
            </div>
            <div class="form-control">
                <label>Image URL</label>
                <input type="text" name="imageUrl" id="imageUrl" value="<%= product.imageUrl %>"/>
            </div>
            <div class="form-control">
                <label>Price</label>
                <input type="number" name="price" id="price" step="0.01" value="<%= product.price %>"/>
            </div>
            <div class="form-control">
                <label>Description</label>
                <textarea name="description" id="title" rows="3"><%= product.description %></textarea>
            </div>
            <!-- hidden field to store the ID in case of edit -->
            <% if (action === 'edit') { %>
                <input type="hidden" name="productId" id="productId" value="<%= product._id %>"/>
            <% } %>
            <div class="button-div">
                <button type="submit">Send</button>
            </div>
        </form>
    </main>
</body>
</html>